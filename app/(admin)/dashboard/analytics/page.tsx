"use client";

import withAuth from "@/components/HOC/withAuth";

const AnalyticsPage = () => {
  return (
    <div>
      <h1>Analytics</h1>
    </div>
  );
};

export default withAuth(AnalyticsPage);
